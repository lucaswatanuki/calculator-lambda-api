server:
  port: 8082

spring:
  profiles:
    active: ${PROFILE}

url: http://localhost:8082/
bcb-url: ${BCB_API}

logging:
  level:
    com:
      installments:
        calculator:
          client:
            CalculatorClient: DEBUG
            BcbClient: DEBUG