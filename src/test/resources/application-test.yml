server:
  port: 8082

spring:
  profiles:
    active: ${PROFILE}
  data:
    mongodb:
      authentication-database: ${DB_AUTH}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
      database: ${DB_NAME}
      port: ${DB_PORT}
      host: ${DB_HOST}

url: http://localhost:8082/
bcb-url: ${BCB_API}